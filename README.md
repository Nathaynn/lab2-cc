Schedules and output files are within their respective directories. 

# How to Run 
Same commands as instructions

python sim.py --cc 2pl --schedule schedules/s1.jsonl --out out/s1_2pl

python sim.py --cc mvcc --schedule schedules/s1.jsonl --out out/s1_mvcc
